<h1>Books</h1>

<% @books.each do |book| %>

<div>

  <p>image:<%= image_tag 'no_image.jpg' %></p>
  <p>name:<%= book.user.name %></p>

  <p>Title:<%= link_to book_path(book.id) do %><%= book.title %></p>
  <% end %>

  <p>Opinion:<%= book.body %></p>
</div>

<% end %>